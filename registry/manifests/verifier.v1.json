{
  "name": "verifier",
  "version": "v1",
  "kind": "inproc",
  "entrypoint": "symphonia.tools.stubs.verifier.factory",
  "input_schema": {
    "type": "object",
    "required": ["entities"],
    "properties": {
      "entities": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["mention", "entity"],
          "properties": {
            "mention": {"type": "string"},
            "entity": {"type": "string"}
          },
          "additionalProperties": false
        }
      }
    },
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "required": ["triples"],
    "properties": {
      "triples": {
        "type": "array",
        "items": {
          "type": "array",
          "items": {"type": "string"},
          "minItems": 3,
          "maxItems": 3
        }
      }
    },
    "additionalProperties": false
  }
  ,
  "model": {
    "base_id": "stub",
    "adapter_uri": "file://stub",
    "loader": "peft-lora"
  }
}
